import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
    padding: 40,
  },
  text: {
    fontSize: 40,
    fontWeight: 'bold',
    color: '#fff',
  },
  textBig: {
    fontSize: 80,
    fontWeight: 'bold',
    color: '#fff',
  }
});
